{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--config", "playwright-mcp-config.json",
        "--init-script", "playwright-mcp-init.js"
      ]
    }
  }
}
